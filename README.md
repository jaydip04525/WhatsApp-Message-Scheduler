# 📩 WhatsApp Message Scheduler (Python)

This project allows you to **automatically send WhatsApp messages** to single or multiple contacts using Python.  
It uses the `pywhatkit` library to open WhatsApp Web and delivers messages **at the exact scheduled time**.

---

## 🚀 Features

- ⏱️ **Schedule messages** for any date and time  
- 📞 **Send to multiple phone numbers**  
- ⚡ **Instant message sending** using `sendwhatmsg_instantly()`  
- 🕒 Automatically checks if the scheduled time has already passed  
- 🖥️ Works directly on **WhatsApp Web**

---

## 🧾 Requirements

Make sure you have the following installed:

```bash
pip install pywhatkit


## Also required:

Python 3.7+

Stable internet connection

Logged-in WhatsApp Web account
